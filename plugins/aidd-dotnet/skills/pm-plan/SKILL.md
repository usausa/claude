---
name: pm-plan
description: feature 単位 (1 feature ≒ 1 SPEC) でイテレーション計画を作成・更新する。WBS/フェーズゲートは使わない。
---

> 引数: [イテレーションの狙い]

`pm` サブエージェントに、feature 単位のイテレーション計画を依頼する。

- `docs/spec/` の SPEC を backlog として、`docs/pm/iteration-plan.md` を更新する (feature 単位、状態付き)。
- 方針は `docs/pm/README.md` が正。
